lilliputian is a dead simple URL shortener that redirects all URLs in its directory.

Installation:
1. Create a database
2. Run the SQL in lilliuptian.sql to create the table
3. Copy config.php.default to config.php
4. Fill in the values in config.php

That's it!

Usage:
1. Navigate to manage.php in your installation.
2. Enter the password you stored in $admin_password in config.php
3. You can now create, manage, and view stats for lilliputs from this page
4. There's also a bookmarklet that will shorten the current URL

TODO:
* automatic key generation for when you don't want to name it
* stylesheet
* edit and delete lilliputs